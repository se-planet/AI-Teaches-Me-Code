# 数据结构与算法 学习笔记

> 📅 开始时间：2026-07-01
> 📖 参考：[Hello 算法](https://www.hello-algo.com/) · [算法通关手册](https://algo.itcharge.cn/)

---

## 📌 待学习清单

- [x] 数组 ✅ 2026-07-01
- [x] 链表 ✅ 2026-07-03
- [x] 栈与队列 ✅ 2026-07-09
- [x] 哈希表 ✅ 2026-07-10
- [ ] 树（二叉树、二叉搜索树）
- [ ] 堆
- [ ] 图
- [ ] 搜索（BFS、DFS）
- [ ] 排序算法（快速排序、归并排序等）
- [ ] 分治
- [ ] 回溯
- [ ] 动态规划
- [ ] 贪心

---

## ✅ 一、数组（Array）

> 📅 学习时间：2026-07-01 ~ 2026-07-02
> 📂 代码：[01_数组/lesson_array.py](01_数组/lesson_array.py)

### 1.1 核心概念

| 特性 | 说明 |
|------|------|
| **存储方式** | 一块连续的内存空间，像一排紧挨的停车位 |
| **索引访问 O(1)** | 知道首地址 + 偏移量，直接算出任意元素位置 |
| **插入/删除 O(n)** | 中间插入要挪动后面所有元素 |
| **查找 O(n)** | 最坏情况要遍历整个数组 |

### 1.2 时间复杂度速查

| 操作 | 复杂度 | 原因 |
|------|:--:|------|
| 按索引读 | O(1) | 连续内存，直接计算 |
| 按索引写 | O(1) | 同上 |
| 尾部插入 `append` | O(1) | 末尾有空位 |
| 中间插入 `insert` | O(n) | 后面元素要后移 |
| 按值查找 | O(n) | 一个个遍历 |
| 删除 | O(n) | 后面元素要前移 |

### 1.3 核心技巧：双指针

两个指针从不同位置出发，向中间（或同向）移动。

**常见变体：**
- **左右指针**：反转数组
- **快慢指针**：去重、移动零

**模板：**
```python
left, right = 0, len(arr) - 1
while left < right:
    arr[left], arr[right] = arr[right], arr[left]  # 交换
    left += 1
    right -= 1
```

### 1.4 两数之和（Two Sum）详解

> LeetCode 第 1 题，新手必刷

**暴力法 O(n²)：** 每两个数都加一遍试试。

```python
for i in range(len(nums)):
    for j in range(i + 1, len(nums)):
        if nums[i] + nums[j] == target:
            return [i, j]
```

**哈希法 O(n)：** 把加法变成减法，算搭档，查字典。

```python
seen = {}
for i, num in enumerate(nums):
    complement = target - num
    if complement in seen:
        return [seen[complement], i]
    seen[num] = i
```

**关键：先查后存。** 先 `seen[num]=i` 再查会导致 `[3,3]` target=6 找到自己。

| | 暴力 | 哈希 |
|------|------|------|
| 思路 | 两两相加试 | 算搭档，翻笔记本 |
| 时间 | O(n²) | O(n) |
| 空间 | O(1) | O(n) |

### 1.5 已刷 LeetCode

| 题号 | 题目 | 技巧 | 状态 |
|:--:|------|------|:--:|
| 1 | 两数之和 | 暴力 + 哈希（双解 AC） | ✅ |
| 26 | 删除有序数组重复项 | 快慢指针 | ✅ |
| 283 | 移动零 | 快慢指针 | ✅ |

### 1.6 Python 注意点

- `list.sort()` 返回 `None`（原地排序），`sorted(list)` 返回新列表
- 切片是左闭右开：`arr[a:b]` 含 a 不含 b
- `arr2 = arr1` 是浅拷贝 → 用 `arr1[:]` 或 `arr1.copy()`
- 遍历时不要修改数组本身

---

## ✅ 二、链表（Linked List）

> 📅 学习时间：2026-07-03
> 📂 代码：[02_链表/lesson_linked_list.py](02_链表/lesson_linked_list.py)

### 2.1 链表 vs 数组

| | 数组 | 链表 |
|------|------|------|
| 内存 | 连续一块 | 散落各处，指针相连 |
| 索引访问 | O(1) 瞬间 | O(n) 必须从头走 |
| 插入/删除 | O(n) 挪位置 | O(1) 改指针就行（头部） |
| 查找 | O(n) | O(n) |
| 比喻 | 停车场一排紧挨车位 | 火车，车厢用钩子相连 |

### 2.2 节点结构

```python
class ListNode:
    def __init__(self, val=0, next=None):
        self.val = val      # 数据
        self.next = next    # 指向下一个节点的指针
```

### 2.3 基本操作

| 操作 | 复杂度 | 关键点 |
|------|:--:|------|
| 头部插入 | O(1) | 新节点 → 旧头 |
| 尾部插入 | O(n) | 走到末尾再挂 |
| 删除 | O(n) | 先找到，跳过它 |
| 遍历 | O(n) | `while cur:` |

### 2.4 核心技巧

**反转链表（四步公式）：**

```python
prev = None; cur = head
while cur:
    nxt = cur.next      # ① 记后路
    cur.next = prev     # ② 箭头调头（核心）
    prev = cur          # ③ prev 前进
    cur = nxt           # ④ cur 前进
return prev
```

**快慢指针：**

| 应用 | 技巧 |
|------|------|
| 找中点 | 快走两步、慢走一步 |
| 判环 | 快慢指针相遇 = 有环 |

### 2.5 关键原则

- **head 不动，cur 走路** — head 是门牌号，丢了就找不到链表了
- **改指针前先记后路** — `nxt = cur.next` 后再改 `cur.next`
- **空链表先判断** — `if not head: return`
- **dummy 看门，cur 干活** — dummy 守在起点不动

### 2.6 LeetCode 实战

| 题号 | 题目 | 技巧 | 状态 |
|:--:|------|------|:--:|
| 206 | 反转链表 | 迭代 + 递归（双解 AC） | ✅ |
| 141 | 环形链表 | 快慢指针 | ✅ |
| 21 | 合并两个有序链表 | 双指针 + dummy | ✅ |
| 19 | 删除倒数第N个节点 | 快慢指针 | ✅ |

### 2.7 递归反转链表（进阶）

```python
def reverseList(self, head):
    if not head or not head.next:
        return head
    new_head = self.reverseList(head.next)
    head.next.next = head
    head.next = None
    return new_head
```

### 2.8 Python 注意点

- `cur.next` 读是取值，`cur.next = xxx` 是改箭头
- `while cur` 遍历每个节点，`while cur.next` 停在最后一个
- LeetCode 上 ListNode 已定义好，本地需自己定义

---

## ✅ 三、栈与队列（Stack & Queue）

> 📅 学习时间：2026-07-09
> 📂 代码：[03_栈与队列/lesson_stack_queue.py](03_栈与队列/lesson_stack_queue.py)

### 3.1 核心概念

| | 栈（Stack） | 队列（Queue） |
|------|------------|-------------|
| 规则 | LIFO 后进先出 | FIFO 先进先出 |
| 插入 | `append()` O(1) | `append()` O(1) |
| 删除 | `pop()` O(1) | `popleft()` O(1) |
| 看顶端 | `stack[-1]` | `queue[0]` |
| 比喻 | 一摞盘子，后放先拿 | 排队买票，先来先走 |
| Python | `list` | `collections.deque` |

### 3.2 关键要点

- **栈用 `list`**：`append()` + `pop()` 都是 O(1)
- **队列用 `deque`**：`popleft()` 是 O(1)，`list.pop(0)` 是 O(n) ❌
- `deque` 底层是双向链表，头部删除不用挪

### 3.3 经典应用

**括号匹配（LeetCode 20）：**

```python
def isValid(s):
    stack = []
    pairs = {')': '(', ']': '[', '}': '{'}
    for ch in s:
        if ch in pairs:
            if not stack or stack.pop() != pairs[ch]:
                return False
        else:
            stack.append(ch)
    return not stack
```

**最小栈（LeetCode 155）：** 两个栈同步维护，一个存数据，一个存当前最小值。

**双栈模拟队列（LeetCode 232）：** 两个栈来回倒，反转两次恢复原序。

### 3.4 LeetCode 实战

| 题号 | 题目 | 技巧 | 状态 |
|:--:|------|------|:--:|
| 20 | 有效的括号 | 栈匹配 | ✅ |
| 155 | 最小栈 | 双栈同步 | ✅ |
| 232 | 用栈实现队列 | 双栈倒数据 | ✅ |

---

## 🔥 四、哈希表（Hash Table）

> 📅 学习时间：2026-07-10
> 📂 代码：[04_哈希表/lesson_hash_table.py](04_哈希表/lesson_hash_table.py)
> ⚡ 本章最快——dict 你本来就会用

### 4.1 核心思想

> key → 哈希函数 → 数组下标 → O(1) 查到

Python 的 `dict` 和 `set` 底层就是哈希表。

### 4.2 三板斧

| 用法 | 结构 | 场景 |
|------|:--:|------|
| 判重 | `set` | 有没有出现过？ |
| 计数 | `dict` | 出现了几次？ |
| 映射 | `dict` | key → value |

### 4.3 常用操作

```python
# dict
d = {}
d[key] = val         # 插入
d.get(key, default)  # 安全取值
key in d             # O(1) 判断存在

# set
s = set()
s.add(x)             # 插入
x in s               # O(1) 判断存在
```

### 4.4 LeetCode 实战

| 题号 | 题目 | 技巧 | 状态 |
|:--:|------|------|:--:|
| 217 | 存在重复元素 | set 判重 | ✅ |
| 242 | 有效的字母异位词 | dict 计数 | ✅ |

### 4.5 比较规则

| 类型 | `==` 比较 |
|------|----------|
| `list` | 顺序要一样 |
| `set` / `dict` | 不看顺序，只看内容 |

### 4.6 复杂度

dict/set 的插入、删除、查找平均都是 **O(1)**。

---